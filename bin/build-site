#!/usr/bin/env bash
set -euo pipefail

if [ ! -w "${HOME:-/}" ]; then
  export HOME="${PWD}"
fi

export BUNDLE_APP_CONFIG="${PWD}/.bundle"
export BUNDLE_PATH="${PWD}/vendor/bundle"

bundle exec jekyll build "$@"
