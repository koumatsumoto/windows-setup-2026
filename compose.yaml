services:
  site:
    image: ruby:3.3
    working_dir: /app
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - .:/app
    ports:
      - "127.0.0.1:4000:4000"
    command: >
      bash -lc "./bin/setup-site &&
      ./bin/serve-site --host 0.0.0.0 --port 4000 --destination _site-local"
